{
  "name": "estrela-photo-dev",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling for estrela.photo (tests + QA). The site itself is static and has no build step.",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "serve": "python3 -m http.server 5503 --bind 127.0.0.1",
    "lighthouse": "lighthouse http://localhost:5503/ --view"
  },
  "devDependencies": {
    "jsdom": "^25.0.1",
    "vitest": "^2.1.8"
  }
}
